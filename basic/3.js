"use strict" //tret all JS code as newer version
//alert(3+3) // we re using node js not brower thats why this will giv us r` 
console.log(typeof [1,2,3]);
let a=Array.isArray([1,2,3]);
console.log(a);
